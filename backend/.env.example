# Copy to .env and fill in values before running the API.
# NEVER commit .env to version control.

# PostgreSQL connection string (development database)
DATABASE_URL=postgresql+psycopg2://neurocortex:neurocortex@localhost:5432/neurocortex

# Isolated database for pytest only (created automatically; never overwrites DATABASE_URL data)
TEST_DATABASE_URL=postgresql+psycopg2://neurocortex:neurocortex@localhost:5432/neurocortex_test

# JWT — use a long random secret in production (e.g. openssl rand -hex 32)
JWT_SECRET=neurocortex-dev-secret-7f93a8d2-long-random-key
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=60

# App
ENVIRONMENT=development
API_PREFIX=/v1

# Study mode: development | pilot | production
STUDY_MODE=development

# Optional overrides. When omitted:
# - SHOW_TEST_DATA defaults to true in development, false otherwise
# - ALLOW_PARTICIPANT_PREDICTIONS defaults to false
# SHOW_TEST_DATA=true
# ALLOW_PARTICIPANT_PREDICTIONS=false

# Synthetic integration-test identifiers to hide in pilot/production
BLOCK_SYNTHETIC_PREFIXES=MLSEED,MLPRED,MLSHAP
SYNTHETIC_DATASET_PREFIX=phase-2

# Study timezone for weekly session progress (Monday–Sunday)
STUDY_TIMEZONE=America/New_York

# Groq-hosted participant feedback (backend only — never expose to frontend)
GROQ_API_KEY=
GROQ_MODEL=
GROQ_TIMEOUT_SECONDS=30
GROQ_MAX_RETRIES=2

# Optional SMTP for researcher bulk email (separate message per recipient)
# SMTP_HOST=
# SMTP_PORT=587
# SMTP_USERNAME=
# SMTP_PASSWORD=
# SMTP_FROM_EMAIL=
# SMTP_USE_TLS=true

# Golden Vault demo overrides (never commit real hash or plaintext code)
GOLDEN_VAULT_ENABLED=false
GOLDEN_VAULT_CODE_HASH=
GOLDEN_VAULT_TOKEN_MINUTES=30
# When false, enabled overrides remain stored but are not applied to dashboards
# GOLDEN_OVERRIDES_VISIBLE=true
GOLDEN_AUTO_SESSIONS_ENABLED=true
GOLDEN_AUTO_SESSION_MAX_CATCHUP_DAYS=30
GOLDEN_AUTO_SESSION_BATCH_SIZE=100

# REQUIRE_CONSENT_FOR_SESSIONS defaults to false in development, true in pilot/production
# REQUIRE_CONSENT_FOR_SESSIONS=false
ACTIVE_STUDY_PROTOCOL_VERSION=2026-pilot-v1
ALLOW_RESEARCHER_CONSENT_OVERRIDE=false
